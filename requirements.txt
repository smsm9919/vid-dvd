fastapi>=0.115,<1
uvicorn[standard]>=0.34,<1
httpx>=0.28,<1
python-dotenv>=1.0,<2
pydantic>=2.10,<3

# Test runner. Declared so a fresh checkout can run the test suite
# (``python -m pip install -r requirements.txt``) without a separately
# installed global pytest.
pytest>=8,<10

# Phase 13: optional free-first providers.
# piper-tts is GPL-3.0 (OHF-Voice/piper1-gpl) and CPU-friendly. It is an OPT-IN
# adapter — only loaded when PIPER_ENABLED=true. Install separately if you want
# local TTS:  pip install piper-tts
piper-tts>=1.7; python_version >= "3.9" and extra == "piper"
# kokoro-onnx is MIT; Kokoro-82M model weights are Apache 2.0 (commercial-safe,
# no copyleft). Preferred free-first TTS. Opt-in via KOKORO_ENABLED=true.
# Model files (~88MB int8 + 27MB voices) downloaded out-of-band to models/kokoro.
kokoro-onnx>=0.5; python_version >= "3.9" and extra == "kokoro"
soundfile>=0.12; python_version >= "3.9" and extra == "kokoro"
